; NODE_ENV=production is set globally on this machine, which makes npm skip
; devDependencies (tsx, typescript) and breaks npm test / npm run typecheck.
; Force dev deps for this project.
include=dev
