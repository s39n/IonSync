; NODE_ENV=production is set globally on the dev machine, which makes npm skip
; devDependencies (esbuild, typescript, eslint) and breaks the build/lint.
; Force dev deps for this project.
include=dev